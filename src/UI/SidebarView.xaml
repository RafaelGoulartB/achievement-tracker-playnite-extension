<UserControl x:Class="AchievementTracker.UI.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="Transparent">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Style x:Key="AchievementProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Background" Value="#22FFFFFF" />
            <Setter Property="Foreground" Value="#4CAF50" />
            <Setter Property="Height" Value="6" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                            <Grid x:Name="PART_Track">
                                <Border x:Name="PART_Indicator" Background="{TemplateBinding Foreground}" CornerRadius="3" HorizontalAlignment="Left" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Achievement Tracker" FontSize="26" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,20">
            <TextBlock x:Name="TotalGamesText" Text="Loading library..." Foreground="#88FFFFFF" FontSize="14"/>
        </StackPanel>
        <ListBox x:Name="GamesGrid" Grid.Row="2" Background="Transparent" BorderThickness="0" 
                 HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Width="160" Height="260" Background="#252525" CornerRadius="8" Padding="0" Margin="5">
                        <StackPanel>
                            <Border Height="180" CornerRadius="8,8,0,0" ClipToBounds="True">
                                <Image Source="{Binding CoverPath}" Stretch="UniformToFill" />
                            </Border>
                            <StackPanel Margin="8">
                                <TextBlock Text="{Binding GameName}" FontWeight="Bold" FontSize="11" Foreground="White" TextTrimming="CharacterEllipsis"/>
                                
                                <!-- Real Achievement Progress Bar -->
                                <ProgressBar Style="{StaticResource AchievementProgressBarStyle}" 
                                             Value="{Binding Percentage}" 
                                             Margin="0,4" />

                                <TextBlock Text="{Binding StatusText}" FontSize="10" Foreground="#88FFFFFF" Margin="0,4,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ProgressBar x:Name="GlobalProgressBar" Grid.Row="2" Height="4" VerticalAlignment="Top" 
                     IsIndeterminate="True" Visibility="Collapsed" Background="Transparent" Foreground="#4CAF50"/>
    </Grid>
</UserControl>
