<UserControl x:Class="AchievementTracker.UI.AchievementTrackerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Border Background="#08FFFFFF" MinHeight="60" Padding="6" CornerRadius="6">
        <StackPanel>
            <!-- Status / Empty State (hidden by default) -->
            <TextBlock x:Name="StatusText" Text="Loading..." 
                       Foreground="#88FFFFFF" FontSize="12" HorizontalAlignment="Center" Margin="0,10"
                       Visibility="Collapsed"/>

            <!-- Progress Header -->
            <StackPanel Orientation="Horizontal" Margin="0,4,0,4">
                <TextBlock x:Name="UnlockCountText" Text="0/0" FontSize="15" FontWeight="Bold" Foreground="White" />
                <TextBlock x:Name="PercentText" Text=" (0%)" FontSize="15" Foreground="#88FFFFFF" />
            </StackPanel>

            <!-- Progress Bar (Thicker HLTB style) -->
            <Border Height="12" Background="#22FFFFFF" CornerRadius="6" Margin="0,4,0,12">
                <Grid>
                    <Border x:Name="ProgressBarFill" Background="#4CAF50" CornerRadius="6" HorizontalAlignment="Left" Width="0" />
                </Grid>
            </Border>

            <!-- Achievement List -->
            <ListBox x:Name="AchievementsListBox" Background="Transparent" BorderThickness="0"
                     HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MaxHeight="400" Visibility="Collapsed">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <!-- <Border Padding="8" Margin="0,0,0,4" Background="#44000000" CornerRadius="8"> -->
                        <Border Padding="8" Margin="0,0,0,4" Background="#66000000" CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="36"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="32" Height="32" Background="#333" CornerRadius="3" VerticalAlignment="Center" Grid.Column="0">
                                    <Image Source="{Binding IconUrl}" Stretch="UniformToFill">
                                        <Image.Style>
                                            <Style TargetType="Image">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsUnlocked}" Value="False">
                                                        <Setter Property="Opacity" Value="0.4" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Image.Style>
                                    </Image>
                                </Border>
                                <StackPanel Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="12" Foreground="White" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Description}" FontSize="10" Foreground="#AAAAAA" TextWrapping="Wrap" Margin="0,1,0,0"/>
                                    <TextBlock Text="✓ Unlocked" FontSize="10" Foreground="#4CAF50" FontWeight="Bold"
                                               Visibility="{Binding IsUnlocked, Converter={StaticResource BooleanToVisibilityConverter}}"
                                               Margin="0,2,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Border>
</UserControl>
